name: lumi
description: "LUMI - Eye Health Monitoring App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0' # Widened constraint to support all plugins

dependencies:
  flutter:
    sdk: flutter
  
  # --- CORE DEPENDENCIES ---
  cupertino_icons: ^1.0.8
  permission_handler: ^12.0.1
  path_provider: ^2.1.2
  wakelock_plus: ^1.2.0
  local_auth: ^2.3.0
  
  # --- CAMERA & IMAGING ---
  camera: ^0.11.0
  image_picker: ^1.0.7
  path: ^1.9.0
  sqflite: ^2.4.1
  flutter_secure_storage: ^9.2.2
  connectivity_plus: ^6.1.4
  http: ^1.2.2
  crypto: ^3.0.6
  
  # --- AI & ML ---
  google_mlkit_face_detection: ^0.11.0
  # face_mesh_detection removed — dual pipeline disabled for performance (T1)
  
  # --- WEBVIEW ---
  webview_flutter: ^4.4.2
  
  # --- HAPTIC & AUDIO FEEDBACK ---
  vibration: ^3.0.0
  just_audio: ^0.9.36
  
  # --- CHARTS & VISUALIZATION ---
  fl_chart: ^0.68.0
  shared_preferences: ^2.5.3
  qr_flutter: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  assets:
    # AI model assets removed
    - assets/mascot/
    - assets/media_hub/
    - assets/app_icons/
    - assets/icons/arcade/

  fonts:
    - family: SuperJoyful
      fonts:
        - asset: assets/fonts/SuperJoyful-lxwPq.ttf
    - family: ClanPro
      fonts:
        - asset: assets/fonts/FFClanProRegular.TTF
          weight: 400
        - asset: assets/fonts/FFClanProMedium.TTF
          weight: 500
